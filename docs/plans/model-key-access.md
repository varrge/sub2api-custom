# 按模型管理密钥权限

已确认需求：用户在 API 密钥工具栏进入独立「按模型管理」页面，选择一个精确公开模型 ID，查看并修改自己全部未删除 Key 对此模型的权限。现有单 Key 编辑与新页面读取、写入相同的 `api_keys.model_allowlist`，没有模型到用户的独立授权表。

## 行为

- 默认按当前策略勾选。搜索、分组筛选只影响显示；批量保存包含已加载的全部 Key。页面不显示密钥凭据。
- 候选合并可用分组目录和已保存模型，允许手动输入完整 ID。目录不是调用资格保证；已停用、过期以及目录未列出会单独标注。
- 不限制 → 禁止某模型：启用 deny，名单只有该模型，不启用暂存的旧名单；allow 加入/移除所选模型；deny 移除/加入所选模型。其余模型及未来模型权限不变。
- 空的启用 allow 是禁止全部；关闭限制是允许所有。新建 Key 沿用其自身配置，不继承以前的批量操作。
- 有草稿时切换模型、刷新或离开页面需选择保存、放弃、留在原页。保存失败保留草稿；冲突需刷新核对。

## 接口与一致性

- `GET /api/v1/keys/model-access` 返回当前用户全部未删除 Key 的管理摘要、模型规则及策略修订值，最多 10000 把，不含凭据。
- `PUT /api/v1/keys/model-access` 接收 `{model, entries:[{id, revision, allowed}]}`。操作的是读出时的 Key 快照；后来新建的 Key 不自动加入，后来删除或修改策略的 Key 使保存返回 409。
- 后端按 ID 顺序加行锁，在同一事务内校验归属、比对策略修订值、逐 Key 更新规则。任何验证/写入失败回滚整批；只写模型配置和更新时间，不回写用量、分组、状态。
- 修订值针对 Key ID、归属和模型策略的摘要；用量和最后使用时间变化不制造冲突。现有用户及管理员单 Key 编辑也携带此值，旧客户端省略仍兼容。
- 提交后在独立有界 context 中清理鉴权缓存并广播，客户端断开不会取消这一步。现有网关的精确 ID 校验与模型目录过滤继续生效。
- 复用已有 JSON 字段，无新增数据库迁移。空 allow 行为改变在规范化与单 Key UI 中同步实现。

## 分工与验证

Kimi 负责 `ModelKeyAccessPanel.vue`、工具栏入口、路由、中英文文案、空名单提示及 UI 测试。Codex 负责接口、权限转换、数据库事务、冲突处理、数据加载与导航草稿逻辑。

定向服务/handler/网关/仓库测试；使用独立本地 PostgreSQL schema 测试事务回滚、所有权、删除、超限、锁等待后的冲突、并发用量与修订值；前端测试验证隐藏行、草稿、手动模型、错误与导航；浏览器以模拟接口检查桌面/手机、中英文与明暗主题。未连接或修改生产。

## 完成验证（2026-09-23）

- 前端六个相关测试文件共 72 项通过；`pnpm run build`（含中英文键校验、TypeScript 和 Vite）通过；所有修改的前端文件通过 ESLint。
- 后端 API Key、模型校验、网关和仓库定向测试通过；`TEST_MODEL_ACCESS_DATABASE_URL` 指向本地独立 PostgreSQL 时，事务集成测试通过。
- 浏览器使用模拟接口检查 1440px 桌面、390px 手机、320px 小屏、中英文与明暗主题：无横向溢出或运行时错误；保存冲突在弹窗内可见且保留草稿。另验证批量保存后单 Key 编辑读到空 allow，再在单 Key 允许模型后按模型页面读回修改。
- 独立代码复查通过；已修复提交后浏览器断开导致缓存清理取消，以及弹窗遮挡保存错误两项问题。
- 无数据库迁移。工作树 `sub2api-model-access`，分支 `feat/model-key-access`，基于 `a3e21abe8`。随后按用户授权完成中国测试实例部署，并准备正式标签 `v0.2.7-custom.11`，见 [部署记录](../../notes/model-key-access-cn-deployment.md)。
