# 中国 staging 0.2.4 适配记录

本功能从原 `custom/0.2.0` 工作区移植到独立分支 `feat/cn-group-buy-0.2.4`，基线是 `1799d7d706b0858ef964dffda7640c8136dd58a3`（中国服务器生图独立超时修复）。原工作区与其中已有的定价改动保留，没有整体部署旧版本。

- 新增数据库迁移编号为 `238_month_card_core.sql`、`239_month_card_billing.sql`，避开 0.2.4 的既有迁移。
- 保留 0.2.4 的推理强度倍率、上游请求 ID、WebSocket 每轮检查、支付回查、图片独立超时。
- 已补齐模型广场对月卡所属分组的可见性，与旧订阅保持一致；对应数据库失败时返回错误，不降级为匿名可见性。
- 修复商品列表重试按钮的中英文翻译引用。
- CI 新增独立 PostgreSQL 实例，实际执行月卡并发、全量迁移及支付恢复测试，避免未配置 DSN 时跳过数据库测试。

2026-09-10 本地验证：后端完整单元测试、完整集成测试通过；独立 PostgreSQL 18.1 上月卡 race 测试通过（全量迁移重复执行、发卡、退款恢复及并发分摊）；前端 291 文件、2129 测试通过，类型检查、lint、构建通过；部署脚本检查通过。模型广场补充回归测试通过。独立代码审查无遗留 Critical/Important 问题。

拟部署版本 `0.2.4-custom.3-candidate.1`。切换前必须等待本提交 GitHub CI 成功，保存 staging 新备份与精确回滚镜像。沿用图片 600 秒、文字 10 秒超时，仅重建应用，保留 PostgreSQL/Redis 和 internal 网络。

月卡迁移保留旧表与唯一约束；新增表以及旧订阅重置记录触发器不要求回滚数据库。旧应用可以启动，但不识别新卡权益；应用回滚期间不应继续月卡业务。服务器实测及实际切换记录另附部署验收报告。
