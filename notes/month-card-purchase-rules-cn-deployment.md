# 月卡购买规则：中国测试实例部署

2026-09-23，按用户“部署中国服务器”指令部署到现有中国隔离测试实例。

## 部署版本

- 工作分支：`feat/cn-festival-leaderboard`。
- 基础提交：`b16b3176550bd835fc65f303092176183378e4ab`；产物包含本次尚未提交的规则功能代码，以源代码压缩包及逐文件清单记录实际内容。
- 版本：`0.2.7-custom.9-candidate.rules.9558f880fb6a`。
- 镜像：`sub2api-custom:0.2.7-custom.9-candidate.rules.9558f880fb6a`。
- 镜像 ID：`sha256:6501c7df4e6306d43cbba9316e3aca3d6b784a3ee8f30b7bd991d8b50c1958a8`。
- 二进制 SHA256：`0b58302d22cd332d48055f4290b28cbf997b9d0b4adc8c3c5373071872f20e17`。
- 源码包 SHA256：`5964fae7d4a363ec0b4660d231416ac011c84249ac86c31354c7a55668c74c5a`。
- 仅重建 `sub2api-multigroup-cn-sub2api-1`，其他容器 ID 未变；应用仍处于 Internal 隔离网络，无公网端口映射。未合并、打 tag 或部署日本生产环境。

## 测试入口

- 月卡购买：`http://127.0.0.1:18081/purchase?tab=group-buy`，选择商品后，在支付方式下方查看规则。
- 后台：`http://127.0.0.1:18081/admin/group-buy` → 规则管理。
- 本机 SSH 隧道：`18081` → 中国测试应用 `172.26.0.4:8080`；ControlPath 为 `/private/tmp/sub2api-catalog-cn.sock`。

## 备份与回滚

远端部署目录：`/home/yinan/sub2api-staging/rules-20260923T080149Z`。

`backup/` 保留部署前 PostgreSQL dump（65,384,489 字节，目录读取及 SHA256 校验通过）、应用数据、原 Compose 配置和应用 inspect。配置及 inspect 为私有文件，不应公开。

旧应用已保留为 `sub2api-restore:rules-20260923t080149z`，其二进制哈希与切换前运行版本一致。迁移 247 仅新增独立规则表；故障回滚仅切换应用镜像，保留数据库，禁止直接覆盖恢复数据库。旧应用没有本次购买规则确认门禁。

为腾出备份和构建空间，逐文件核验归档后，仅删除五份旧版本与本次部署的解压 `image/` 构建目录。压缩包、镜像和备份保留；完成时剩余空间约 742 MiB。

## 验证结果

- 本地：103 项前端测试、针对性 ESLint、类型检查和生产构建通过；Go 支付/handler 测试、真实 PostgreSQL 规则测试和订单同意证据事务测试通过。
- 新应用约 8 秒进入 healthy，后续检查重启数为 0；部署版本及二进制哈希一致。
- 迁移 `247_month_card_purchase_rules.sql` 已执行，数据库记录的迁移校验和与源码归档一致。
- 默认发布第 1 版《拼团规则》《购买须知》，退款文案与用户指定内容一致。
- 未登录访问规则被拒绝，普通用户无法读取、修改或发布后台规则；用户、密钥、订阅、用量和商品接口通过。
- 部署前后 users/accounts/api_keys/user_subscriptions 的总数及未删除数一致。
- 实际隧道浏览器检查：1366、390、320 像素下购买页和规则管理均无横向溢出；手机规则弹窗全屏；规则滚动到底可用“已阅读”；关闭弹窗不计已读；首次勾选和付款门禁正确。
- 线上浏览器验证禁止写入，没有生成订单、确认阅读或修改草稿；全部账号阅读记录仍为空，保留首次体验。
- 此隔离实例未验证真实外部支付及微信授权全流程。

本地证据：`/tmp/sub2api-rules-deploy-20260923T080149Z`，含源码与构建清单、部署记录、API 检查、真实页面截图和 `live-browser.json`。部署前功能测试证据：`/tmp/sub2api-monthcard-consent`。

## 发布前检查

`v0.2.7-custom.10` 以本次部署源码为基础。发布前 Go 静态检查要求显式处理清理调用的返回值，因此 `rules.go` 与其 PostgreSQL 测试中五处 `defer Close/Rollback` 改为仓库通用的忽略清理错误写法；调用时机和业务逻辑不变。中国实例仍运行上文记录的候选镜像。

发布前全量前端测试 2593 项通过，前端 CI 检查、后端全量单元/集成测试及部署脚本、发布门禁检查通过。发布检查证据保留在 `/tmp/sub2api-custom10-release`；远端发布仍须通过标签所指提交的完整 CI。
