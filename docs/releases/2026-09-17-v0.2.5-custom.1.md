# v0.2.5 自定义版本升级记录

更新日期：2026-09-18。订阅/拼团拆分候选版已部署中国 staging；用户测试后确认“可以tag了”，授权提交、推送和正式发布。发布前记录：尚未创建正式 tag，未操作日本生产。

## 基线

- 工作分支：`custom/0.2.5`。
- 自定义基线：`142c7cf9e19dfa6ccb79af73cd84a2b5ec3ed919`（`v0.2.4-custom.7`）。
- 官方版本：`v0.2.5`，发布于 2026-09-15；合并的提交为 `86f93c28ee34cc74b629dafb748bd5ac5ca8c5ea`。
- 计划版本：`0.2.5-custom.1`；需先完成 CI、staging 候选验收，再创建不可变正式 tag。
- 工作在独立 worktree `sub2api-cn-group-buy` 中完成；原 `sub2api-custom` 工作区的未提交修改保留。

## 合并结果

保留官方 OpenCode Zen / GO 平台、三种站点计费模式、订阅批量操作、API Key 批量编辑、原生 Codex Images 和 WebSocket 修复。保留自定义拼团/月卡、冻结规则、退款、工单、生图工作台、顶部快捷菜单和限时倍率。

冲突核对和回归测试修复包括：

- 恢复 OpenCode 前端类型、调度阈值和未指定 service tier 的策略选项。
- 贯通后台站点类型选择器、设置保存、公开 API、页面注入、导航标题、订阅轮询与路由保护。首次合并曾让订阅开关同时隐藏拼团；此决定已由下述独立拆分修订。
- 首次合并曾将订阅与月卡确认流程共用开关；下述拆分将开关限制为官方订阅，保留已有支付恢复流程。
- 恢复自定义页面“隐藏打开按钮”和续费弹窗滚动行为。
- 新 Codex Images 直连与 Responses 回退均使用图片专用超时配置。
- 批量重置旧订阅额度时，月卡计费 generation 和额度更新复用外层 Ent 事务。真实 PostgreSQL 测试覆盖重置后读取失败的回滚、重试以及单连接池，防止接口报告失败但额度已经清零。

自定义更新源 `UPDATE_GITHUB_REPO`、`custom.N` 版本比较、Fork GHCR 路径、稳定 Release/latest 规则以及 Release 不修改 main 的行为均已核对保留。

## 订阅与拼团拆分（已部署中国测试服）

用户明确要求官方订阅与拼团分别管理，并授权部署中国服务器提供隧道测试。中国测试服验收后，用户于 2026-09-18 确认可以打 tag；本次按该授权提交并推送拆分改动，等待最终提交的 GitHub CI 全部通过后创建正式 tag，无需再次合并分支。

- `/subscriptions` 只展示官方订阅，续费恢复进入订阅套餐；顶部订阅摘要仅计算官方订阅。
- 新增 `/my-group-buy`（我的拼团），管理拼团与独购月卡、额度、冻结和解冻；大厅、已参团及月卡付款成功入口均指向对应页面。
- 购买页独立提供充值、订阅、拼团页签。关闭官方订阅不影响拼团；关闭购买入口后仍可管理已有月卡。
- 月卡缓存和轮询独立，退出登录时分别清理。旧月卡链接通过购买方式或订单类型兼容，不再把普通订阅链接解释为拼团。
- 同组消耗顺序保留完整的订阅/月卡引用。共享扣费明细明确标注各项来源，并保留纯余额补扣记录，避免漏掉并发调用结算后的余额费用；后台计费规则未改变。

本地验证：发布前再次执行全量前端 Vitest，312 个文件、2343 项测试全部通过。类型检查、ESLint 和生产构建通过。独立审查发现的扣费记录过滤问题已修正；最终应用源码与中国测试服候选快照完全一致，仅本发布记录更新了部署和授权状态。

## 数据库迁移

本次新增：

- `238_opencode_go_platform.sql`：扩大平台 CHECK 约束。
- `238_purge_unlimited_user_platform_quotas.sql`：删除三个额度均为 NULL 的无限额记录。

既有月卡迁移未改写；迁移器按完整文件名记录，因此与 `238_month_card_core.sql` 不冲突。完整生产迁移在独立本地 PostgreSQL 16 数据库执行，并验证重复运行。中国 staging 的 PostgreSQL 18 已成功执行新增迁移并通过健康和重启检查。静态核对未发现阻止旧程序读取新 schema 的变更；未实际切换旧程序进行回滚演练。

## 验证记录

- 前端类型检查、ESLint、完整 Vitest（310 个文件、2325 项测试）通过；随后新增的 2 项页头模式测试和 3 项后台站点类型保存测试也通过。
- 前端生产构建通过；存在已有的大 chunk 和 Browserslist 数据过期提示。
- 后端重点回归通过：公开计费设置、Grok 视频计费一次性声明、Codex Images、批量订阅操作。
- 独立 PostgreSQL 16 月卡并发、计费、完整迁移、付款/退款恢复及批量重置回滚测试：`go test -race ./internal/monthcard -count=1` 通过，三个 MONTHCARD 测试 DSN 均启用。
- `golangci-lint run --timeout=30m`：0 issues。
- 六项部署脚本检查通过：Apple container 语法与生命周期、Compose 安全、网关环境变量、运行时资源和 Caddy 缓存/流式响应。
- 后端 `make test-unit`、`make test-integration` 和嵌入前端的二进制构建通过。注意：本机没有运行中的 Docker，repository 的容器集成测试会跳过；GitHub CI 会在 PostgreSQL 18 / Redis 容器中实际执行该部分。
- GitHub CI：随本分支推送触发，运行结果需按最终 commit 核对。

## 中国测试部署（2026-09-18）

SSH 已恢复，按用户授权部署 `0.2.5-custom.1-candidate.20260917-separation`。该候选包含基线 `1812f8c5f762ad66f6a55f1acd2386987dd27ca4` 及本次尚未提交的拆分修改，源码快照 SHA-256 为 `25131c2c5d27c7e50e69c885ca1e966e60846d99c317f9f1779e3a4f77b0672e`。基线 GitHub CI 已成功；未提交快照仅有本地测试证据，不作为正式 Release。

- 镜像：`sub2api-custom:0.2.5-custom.1-candidate.20260917-separation`；应用容器：`64cbff9fdaee88cab533db26c769424ca4a48f6509974b7c600ad4b74420b549`。
- 已完成全新 PostgreSQL/Redis/应用数据/配置备份和精确运行镜像备份，所有校验通过；仅 `--no-deps --force-recreate sub2api`。
- 切换后 6.32 秒完成健康验收，重启后约 5.5 秒恢复健康；新增两项官方迁移成功，无 fatal、panic 或迁移失败。
- 核心计数未变：用户 151、账号 57、API Key 219、官方订阅 49、月卡商品 2；数据库、Redis 及其他项目容器 ID 未变，网络保持 internal。
- 隧道入口：<http://127.0.0.1:18080/my-group-buy>、<http://127.0.0.1:18080/subscriptions>、<http://127.0.0.1:18080/purchase?tab=group-buy>。三个页面编译资源哈希与本地构建一致。
- 桌面/手机浏览器登录页检查无 JS 错误、5xx 或横向溢出。用户测试后确认可以正式发布；自动检查未执行真实付款。
- 备份：`/home/yinan/sub2api-staging/backups/20260918T022208Z-pre-0.2.5-custom.1-candidate.20260917-separation`；精确回滚镜像：`sub2api-stage-restore:20260918T022208Z-pre-separation-running`。

完整 Compose 文件顺序、隧道恢复及回滚命令记录于本机 `/Users/varrge/Downloads/Sub2API-中国staging-订阅拼团拆分-20260918.md`。用户已确认正式发布，后续按最终 commit 核对 CI，再发布不可变 tag 并核验 Release 和固定 GHCR 镜像；日本生产切换另行确认。应用回滚不自动恢复数据库。
